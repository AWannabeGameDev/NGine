#version 460 core
#extension GL_ARB_bindless_texture : require

in vec2 uv;

layout (bindless_sampler) uniform sampler2D sampler;
uniform vec4 color;

out vec4 finalColor;

void main()
{
	finalColor = color * texture(sampler, uv);

	if(finalColor.a == 0.0f)
	{
		discard;
	}
}